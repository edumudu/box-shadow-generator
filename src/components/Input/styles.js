import styled from 'styled-components'

export const Group = styled.div `
  
`

export const Input = styled.input `
  width: 100%;
  display: ${props => props.type === 'color' ? 'none' : 'inline-block'}
`
export const Label = styled.label `
  
`
